# abort script on any command that exits with a non zero value
set -e

# I'm not sure why it needs both libjson and libjson-c, but if I leave one out
# the build fails
tar xvzf git/git-2.6.3.tar.gz

echo $BOSH_INSTALL_TARGET

